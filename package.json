{
  "name": "voxel-world",
  "version": "1.0.0",
  "private": true,
  "description": "A playable Minecraft-inspired voxel sandbox game",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "test": "node tests/run.mjs",
    "smoke": "node --import ./tests/register.mjs tests/smoke.mjs",
    "start": "vite --host 0.0.0.0 --port 5173"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
